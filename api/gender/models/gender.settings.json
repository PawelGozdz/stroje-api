{
  "kind": "collectionType",
  "collectionName": "gender",
  "info": {
    "name": "Gender",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "unique": true
    },
    "opis": {
      "type": "richtext"
    },
    "models": {
      "via": "plec",
      "private": true,
      "collection": "model"
    },
    "url": {
      "type": "uid",
      "targetField": "title"
    }
  }
}
